# Required Agentic Skills & Systems

## Core Animations
- Ingest Framer Motion layout animations and layoutId configurations for structural card morphing.
- Implement GSAP (GreenSock) together with ScrollTrigger for handling continuous, smooth scroll scrubbing hooks without performance degradation.

## Engineering Standards
- Strict implementation of CSS perspective matching (`transform-style: preserve-3d`) for card hover inclination tracking.
- Mouse interaction throttling using requestAnimationFrame to ensure high-end hardware acceleration (60fps+ on mobile and desktop).
- Advanced utility components utilising Radix UI primitives for accessible DOM structuring behind the canvas layers.
## 3D Spatial Layout Logic
- Ingest advanced `framer-motion` properties for coordinate spatial interpolation (`useScroll`, `useTransform`, `useSpring`).
- Implement raw pointer-tracking mouse event algorithms utilizing `requestAnimationFrame` to drive smooth tilt transformations without performance lagging.

## Production Performance Optimizations
- Force hardware-accelerated rendering on graphic elements with `will-change: transform, opacity`.
- Implement lazy image composition loading mechanics ensuring fallback placeholder graphics maintain container aspect ratio balance before asset populating.

- Maintain structural layout bounds clean of overflow flickering by containing graphic translation nodes cleanly inside clipping root elements.